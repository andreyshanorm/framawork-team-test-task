export type PaintsResponse = {
  authorId: number;
  created: string;
  id: number;
  imageUrl: string;
  locationId: number;
  name: string;
};

export type AuthorsRespone = {
  id: number;
  name: string;
};
